
- name : Mon playbook
  host: all
  become: true
  remote_user: ubuntu 
  tasks:
    - name : webservers-group caracteristique
      debug:
        msg: {{"server_type"}}
        msg: {{"cloud_provider"}}
        msg: {{"ec2_type"}}
